# E5 · Escalada — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Count the legal deadlines for the person, tell them when one expires, and produce the next step's text with that expiry already stamped on it — so a case does not die of being forgotten.

**Architecture:** The transition table and the deadline arithmetic are pure and live in `packages/core`. The wait itself is a row in `cases.next_deadline_at`, scanned by a job — durable because the database is, not because a worker is. `TaskQueue` stays the port; Trigger.dev slots in later for replay without changing the shape.

## Global Constraints

- **`INV-002`** — the system never sends anything to a channel on the user's behalf, and never handles a third party's credential. It produces text and a deep link; the person clicks.
- **`INV-003`** — the user is the author of every document. `packages/ai/test/invariants/authorship.spec.ts` enforces it.
- **`INV-004` / `INV-005`** — §14.3's vocabulary, via `lintUserFacingText`, on every user-facing string.
- **`INV-008`** — user data through `withUser`. `cases.userId` is NOT NULL, so an anonymous session owns no case; `resolveSession` turns a claimed session id into a user id.
- Money is integer cents. Dates: `timestamptz` for instants, `date` for civil dates. ESM with `verbatimModuleSyntax`. English code and commits; user-facing strings pt-BR. Windows locally, Linux in CI.

**What already exists:** `nextStage`'s closed signature in `packages/core/src/cases/next-stage.ts`, throwing on every input, with `StageEvent`, `StageTransition` and `CaseOutcome`; `Playbook`/`Stage` and §20.2's telecom playbook in the PRD; `cases` with `stage`, `stageEnteredAt`, `nextDeadlineAt`, `workflowRunId`, `protocolToken`, `outcome`, and the partial index `cases_next_deadline`; `case_protocols` and `case_documents`; `assembleContest` and the E4 generator; `withUser`'s case-scoped methods; the `TaskQueue` port with an in-process adapter, and `expire-files` / `rule-lifecycle` as the shape a scheduled job takes here.

---

### Task 1: The national holiday calendar and the deadline calculator

**Files:** `packages/core/src/cases/holidays.ts`, `packages/core/src/cases/deadline.ts`, tests.

RF-181: deadlines come from the issuer's playbook, in calendar days or business days, against the Brazilian national holiday calendar. **Its acceptance is exact: 10 business days started on a Thursday before a holiday must land on the correct date.** Work that example through by hand before writing code, and make it a test.

Brazil's moveable holidays derive from Easter — Carnaval is Easter minus 47 days, Sexta-feira Santa minus 2, Corpus Christi plus 60 — so this needs a computus, not a hardcoded list. The fixed ones are federal law and change by law: treat the calendar as versioned reference data with its source recorded, the way §12.3 treats ANEEL's tables, not as a constant nobody will revisit.

Decide and report: whether the day a deadline starts counts as day zero or day one, what happens when a deadline lands on a holiday or a weekend, and which timezone the arithmetic happens in — a deadline computed in UTC for a person in Brazil can be a day off, and a day is the difference between a valid escalation and a premature one.

Pure, no I/O. `packages/core`'s coverage floor is 90% on all four metrics.

---

### Task 2: `nextStage` — the whole decision table

**Files:** `packages/core/src/cases/next-stage.ts` and `next-stage.table.ts`, tests.

The last function in the core that still throws on every input. §9.1 gives the state machine and names `packages/core/cases/nextStage.table.ts`, **with a test covering every combination of `stage × event × category`**.

Read §9.1's diagram carefully — it carries branches that are easy to miss: `ombudsman` applies only to `card`; `procon` is optional; a case with no protocol for 30 days becomes `stalled`, which §9.1 calls a *sub-state* that returns to `sac` rather than a stage of its own; and any stage, after 60 days without user action, closes as `abandoned`.

`StageTransition` already carries `outcome` and `stampDeadline`. Use Task 1's calculator for `nextDeadlineAt` — or take it as an injected dependency, and say which you chose and why, given `packages/core` does no I/O and the calculator is pure.

An unmapped combination must still throw rather than guess: a wrong stage silently loses someone's case. But after this task there should be no unmapped combination, and the test that enumerates them is what proves it.

---

### Task 3: The durable wait

**Files:** `apps/jobs/src/tasks/case-deadlines.ts`, additions to `withUser`, tests.

RF-180: each case runs a durable workflow, its state mirrored in `cases.stage`. **The acceptance is that restarting the service mid-wait does not lose the case — test it with a simulated deploy.**

The durability comes from the database, not from a worker. `cases.next_deadline_at` and the partial index `cases_next_deadline` have been in the schema since E0 waiting for exactly this: a job scans for deadlines that have passed, applies `nextStage`, stamps the expiry as an event, and schedules the next one.

Write the restart test so it means something: persist a wait, discard every in-memory structure the way a redeploy would, rebuild from the database alone, and assert the case advances correctly. A test that keeps the same process alive proves nothing about a restart.

`ADR-02` picked Trigger.dev for replay and long-running tasks, and that remains right — say plainly in the code what this arrangement does and does not give compared to it, so nobody reads a database scan as a full workflow engine.

---

### Task 4: Recording a protocol, and the next document

**Files:** `apps/web/app/api/cases/[id]/protocol/route.ts`, `apps/web/app/api/cases/[id]/advance/route.ts`, tests.

§8.2 names both. RF-184: posting a protocol releases the wait and schedules the next deadline, **and the workflow resumes in under 30 s**.

RF-182: when a case advances **because a deadline expired**, the generator receives `deadlinesExpired`, and the resulting document names the channel, the protocol number and both dates. E4's `ContestPromptInput` deliberately has no such field — its agent left it out rather than invent a shape that would collide with this block. Define it here, and make the acceptance real: assert the document contains the protocol number and both dates.

`INV-008` applies. `cases.userId` is NOT NULL, so use `resolveSession`; a wrong user and a nonexistent case must be indistinguishable, as every other route here does it.

---

### Task 5: Deep links, and the reminders that do not nag

**Files:** the case screen's channel actions, `apps/jobs/src/tasks/case-reminders.ts`, tests.

**RF-183:** a deep link per channel, the text copied to the clipboard, and the checklist. **`INV-002`'s acceptance is that no data is sent to the channel by the system** — the button copies and opens, and that is all. Write the test that proves nothing leaves.

**RF-185:** reminders in layers, push then e-mail, **suppressed if the user opened the case in the last 24 hours**. The suppression is the requirement: a product that mails someone about a case they just looked at is a product they mute. The `Mailer` port and its local adapter exist; push is E12.

**RF-186:** 30 days with no protocol enters `stalled` with a final reminder; 30 more with no action closes as `abandoned`, with an event. Simulate time rather than waiting for it — `expire-files` shows the injected-clock pattern this codebase uses.

---

### Task 6: The dossier

**Files:** `apps/jobs/src/tasks/dossier.ts` or a route, tests.

RF-187: reaching `jec_ready` produces a chronological PDF carrying every document, protocol and date, plus the attachment list. The acceptance is that the PDF opens and contains the complete timeline.

This is what a person hands to a small-claims court, so what it omits matters as much as what it includes. `INV-007`'s masking applies to everything persisted — work out what a dossier meant for a judge needs that a shared card must not have, and say what you decided. Note that the invoice file itself may already be gone: RF-110's expiry deletes it after 30 days, and a case can outlive that.

Decide how the PDF is produced and justify it against RNF-05's bundle budget if anything lands in the client.

---

## Self-review notes

**Coverage.** RF-180 → Task 3. RF-181 → Task 1. RF-182 → Task 4. RF-183 → Task 5. RF-184 → Task 4. RF-185, RF-186 → Task 5. RF-187 → Task 6. §9.1 → Task 2.

**The two places this block is most likely to be wrong.** Task 1's timezone and day-zero decisions, because a deadline off by one day is an escalation sent too early — arguing from a date the company can disprove. And Task 3's restart test, because the easy version of it keeps the process alive and proves nothing, which is the exact failure this project has shipped five times and caught each time only by deliberately breaking the check.
